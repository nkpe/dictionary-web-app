import { DictionaryResult } from "../types/DictionaryResult"

const ResultsPane = ({dictionaryResult}: {dictionaryResult: DictionaryResult}) => {
    return(
        <div className="results-container border-solid border-cyan-500 rounded-lg w-full flex">
            <></>
        </div>
    )
}